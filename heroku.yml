setup:
  addons:
    - plan: hobby-dev
      as: DATABASE

build:
  docker:
    web: DockerFIle


web: bin/start-pgbouncer-stunnel gunicorn dg.wsgi

release:
  image: web
  command: python manage.py collectstatic