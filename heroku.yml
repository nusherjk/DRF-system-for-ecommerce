setup:
  addons:
    - plan: hobby-dev
      as: DATABASE

build:
  docker:
    web: DockerFIle

run:
    web: bin/start-pgbouncer-stunnel gunicorn dg.wsgi

release:

  command:
    - python manage.py collectstatic
  image: web